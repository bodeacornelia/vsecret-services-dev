version: '3'
services:
  frontend:
    image: bodeanelly/vsecretfrontend
    container_name: frontend
    ports:
      - ${FRONTEND_PORT}:${FRONTEND_PORT}
    depends_on:
      - backend

  backend:
    image: bodeanelly/vsecretbackend
    container_name: backend
    env_file:
      - ./vclothes/.env
    ports:
      - ${BACKEND_PORT}:${BACKEND_PORT}
    depends_on:
      - mysql

  mysql:
    image: mysql:5.7.24
    container_name: mysql
    env_file:
      - .env
    volumes:
      - 'db-data:/var/lib/mysql'
    environment:
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
      - MYSQL_DATABASE=${MYSQL_DATABASE}
    ports:
      - ${MYSQLDB_LOCAL_PORT}:${MYSQLDB_PORT}

volumes:
  db-data:
    driver: local
